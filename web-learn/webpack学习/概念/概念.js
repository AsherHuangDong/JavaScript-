/**
 * * 本质上，webpack 是一个现代 JavaScript 应用程序的静态模块打包器(module bundler)。
 *   当 webpack 处理应用程序时，它会递归的构建一个依赖关系图，其中包含应用程序的每个模块，
 *   然后将所有这些模块打包成一个或多个 bundle。
 * 
 * * webpack 是高度可配置的，它的核心概念包括
 *   1.入口(entry)
 *   2.出口(output)
 *   3.loader
 *   4.插件(plugins)
 */